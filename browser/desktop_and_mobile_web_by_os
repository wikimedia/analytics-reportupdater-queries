#!/bin/bash
beeline -u jdbc:hive2://analytics1015.eqiad.wmnet:10000 -n milimetric --silent=true --outputformat=tsv2 -e "
    SELECT
        '$1' AS date,
        os_family,
        os_major,
        SUM(view_count) as view_count
    FROM wmf.browser_general
    WHERE
        access_method IN ('desktop', 'mobile web') AND
        CONCAT(year, '-', LPAD(month, 2, '0'), '-', LPAD(day, 2, '0')) >= '$1' AND
        CONCAT(year, '-', LPAD(month, 2, '0'), '-', LPAD(day, 2, '0')) < '$2'
    GROUP BY
        '$1',
        os_family,
        os_major
    ORDER BY view_count DESC
    LIMIT 1000
    ;
"
